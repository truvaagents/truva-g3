# Service first — ensures endpoint is available when pods start
---
apiVersion: v1
kind: Service
metadata:
  name: agentic-memory-tool-service
  namespace: truvag3-examples
  labels:
    app: agentic-memory-tool
    component: tool
spec:
  selector:
    app: agentic-memory-tool
  ports:
    - name: http
      port: 80
      targetPort: 8377
      protocol: TCP
  type: ClusterIP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agentic-memory-tool
  namespace: truvag3-examples
  labels:
    app: agentic-memory-tool
    component: tool
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agentic-memory-tool
  template:
    metadata:
      labels:
        app: agentic-memory-tool
        component: tool
    spec:
      containers:
      - name: agentic-memory-tool
        image: agentic-memory-tool:latest
        imagePullPolicy: Never  # For local Kind cluster
        ports:
        - containerPort: 8377
          name: http
        # User-configurable env vars from .env via setup.sh
        envFrom:
        - configMapRef:
            name: agentic-memory-tool-env-config
            optional: true  # Created by setup.sh from .env values
        # Infrastructure env vars (K8s-internal, not from .env)
        env:
        - name: PORT
          value: "8377"
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: REDIS_URL
          value: "redis://redis.truvag3-examples:6379"
        - name: DEV_MODE
          value: "false"
        # Redis Registration Resilience (handles Redis startup race conditions)
        - name: TRUVAG3_DISCOVERY_RETRY
          value: "true"
        - name: TRUVAG3_DISCOVERY_RETRY_INTERVAL
          value: "30s"
        # Logging
        - name: TRUVAG3_LOG_LEVEL
          value: "info"
        - name: TRUVAG3_LOG_FORMAT
          value: "json"
        # K8s identity
        - name: TRUVAG3_K8S_SERVICE_NAME
          value: "agentic-memory-tool-service"
        - name: TRUVAG3_K8S_SERVICE_PORT
          value: "80"
        - name: TRUVAG3_K8S_POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        # Telemetry endpoint (K8s-internal address)
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://otel-collector.truvag3-examples:4318"
        resources:
          requests:
            cpu: "10m"
            memory: "16Mi"
          limits:
            cpu: "100m"
            memory: "64Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8377
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /api/capabilities
            port: 8377
          initialDelaySeconds: 5
          periodSeconds: 10
