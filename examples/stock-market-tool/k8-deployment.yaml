# Note: Secrets are created by setup.sh from .env file
# Run ./setup.sh to deploy with proper API keys
# Or create the secret manually:
#   kubectl create secret generic stock-tool-secrets \
#     --from-literal=FINNHUB_API_KEY=your-key -n truvag3-examples
---
apiVersion: v1
kind: Service
metadata:
  name: stock-tool-service
  namespace: truvag3-examples
  labels:
    app: stock-tool
    component: tool
spec:
  selector:
    app: stock-tool
  ports:
    - name: http
      port: 80
      targetPort: 8338
      protocol: TCP
  type: ClusterIP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stock-tool
  namespace: truvag3-examples
  labels:
    app: stock-tool
    component: tool
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stock-tool
  template:
    metadata:
      labels:
        app: stock-tool
        component: tool
    spec:
      containers:
      - name: stock-tool
        image: stock-tool:latest
        imagePullPolicy: Never  # For local Kind cluster
        ports:
        - containerPort: 8338
          name: http
        envFrom:
        - configMapRef:
            name: stock-market-tool-env-config
        env:
        - name: PORT
          value: "8338"
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: REDIS_URL
          value: "redis://redis.truvag3-examples:6379"
        # Redis Registration Resilience (handles Redis startup race conditions)
        - name: TRUVAG3_DISCOVERY_RETRY
          value: "true"
        - name: TRUVAG3_DISCOVERY_RETRY_INTERVAL
          value: "30s"
        - name: FINNHUB_API_KEY
          valueFrom:
            secretKeyRef:
              name: stock-tool-secrets
              key: FINNHUB_API_KEY
        - name: DEV_MODE
          value: "false"
        - name: TRUVAG3_LOG_LEVEL
          value: "info"
        - name: TRUVAG3_LOG_FORMAT
          value: "json"
        - name: TRUVAG3_K8S_SERVICE_NAME
          value: "stock-tool-service"
        - name: TRUVAG3_K8S_SERVICE_PORT
          value: "80"
        - name: TRUVAG3_K8S_POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        # Telemetry Configuration
        - name: APP_ENV
          value: "development"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://otel-collector.truvag3-examples:4318"
        resources:
          requests:
            cpu: "10m"
            memory: "16Mi"
          limits:
            cpu: "100m"
            memory: "64Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8338
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /api/capabilities
            port: 8338
          initialDelaySeconds: 5
          periodSeconds: 10
