# Workspace Dockerfile for stock-market-tool
# This Dockerfile builds with LOCAL modules (uses local core and telemetry)
# Usage (from truvag3 root): docker build -f examples/stock-market-tool/Dockerfile.workspace -t stock-tool:latest .

FROM golang:1.26.2-alpine AS builder

# Install build dependencies
RUN apk add --no-cache git make gcc musl-dev ca-certificates

WORKDIR /app

# Copy local modules that are referenced by replace directives
COPY core/ ./core/
COPY telemetry/ ./telemetry/

# Copy the stock-market-tool example
COPY examples/stock-market-tool/ ./examples/stock-market-tool/

WORKDIR /app/examples/stock-market-tool

# Configure Go build
ENV GOWORK=off
ENV CGO_ENABLED=0
ENV GOOS=linux
ENV GONOSUMDB=github.com/truvaagents/truva-g3/*

# Download remaining dependencies and build
RUN go mod tidy && go mod download

# Build the binary with optimizations
ARG TARGETARCH=arm64
RUN go build -a -installsuffix cgo \
    -ldflags '-w -s -extldflags "-static"' \
    -o stock-tool .

# Verify the binary was created
RUN ls -lh stock-tool

# Runtime stage
FROM alpine:3.23

# Install runtime dependencies
RUN apk --no-cache add ca-certificates tzdata

WORKDIR /app

# Create non-root user for security
RUN addgroup -g 1001 -S appgroup && \
    adduser -u 1001 -S appuser -G appgroup

# Copy binary from builder stage
COPY --from=builder /app/examples/stock-market-tool/stock-tool .

# Change ownership to non-root user
RUN chown appuser:appgroup /app/stock-tool && \
    chmod +x /app/stock-tool
USER appuser

# Set default PORT if not provided
ENV PORT=8338

# Expose port
EXPOSE ${PORT}

# Run the binary
ENTRYPOINT ["./stock-tool"]
