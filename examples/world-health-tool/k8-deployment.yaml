# Note: No API keys required - WHO GHO and World Bank APIs are free
---
apiVersion: v1
kind: Service
metadata:
  name: world-health-tool-service
  namespace: truvag3-examples
  labels:
    app: world-health-tool
    component: tool
spec:
  selector:
    app: world-health-tool
  ports:
    - name: http
      port: 80
      targetPort: 8368
      protocol: TCP
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: world-health-tool
  namespace: truvag3-examples
  labels:
    app: world-health-tool
    component: tool
spec:
  replicas: 1
  selector:
    matchLabels:
      app: world-health-tool
  template:
    metadata:
      labels:
        app: world-health-tool
        component: tool
    spec:
      containers:
      - name: world-health-tool
        image: world-health-tool:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8368
          name: http
        envFrom:
        - configMapRef:
            name: world-health-tool-env-config
            optional: true  # Created by setup.sh from .env values
        env:
        - name: PORT
          value: "8368"
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: REDIS_URL
          value: "redis://redis.truvag3-examples:6379"
        - name: TRUVAG3_DISCOVERY_RETRY
          value: "true"
        - name: TRUVAG3_DISCOVERY_RETRY_INTERVAL
          value: "30s"
        # NO API KEY NEEDED -- WHO GHO and World Bank APIs are free
        - name: DEV_MODE
          value: "false"
        - name: TRUVAG3_LOG_LEVEL
          value: "info"
        - name: TRUVAG3_LOG_FORMAT
          value: "json"
        - name: TRUVAG3_K8S_SERVICE_NAME
          value: "world-health-tool-service"
        - name: TRUVAG3_K8S_SERVICE_PORT
          value: "80"
        - name: TRUVAG3_K8S_POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: APP_ENV
          value: "development"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://otel-collector.truvag3-examples:4318"
        resources:
          requests:
            cpu: "10m"
            memory: "16Mi"
          limits:
            cpu: "100m"
            memory: "64Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8368
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /api/capabilities
            port: 8368
          initialDelaySeconds: 5
          periodSeconds: 10
