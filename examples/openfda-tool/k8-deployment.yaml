# Note: Secrets are created by setup.sh from .env file
# Run ./setup.sh to deploy with proper API keys
# Or create the secret manually:
#   kubectl create secret generic openfda-tool-secrets \
#     --from-literal=OPENFDA_API_KEY=your-key -n truvag3-examples
---
apiVersion: v1
kind: Service
metadata:
  name: openfda-tool-service
  namespace: truvag3-examples
  labels:
    app: openfda-tool
    component: tool
spec:
  selector:
    app: openfda-tool
  ports:
    - name: http
      port: 80
      targetPort: 8374
      protocol: TCP
  type: ClusterIP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: openfda-tool
  namespace: truvag3-examples
  labels:
    app: openfda-tool
    component: tool
spec:
  replicas: 1
  selector:
    matchLabels:
      app: openfda-tool
  template:
    metadata:
      labels:
        app: openfda-tool
        component: tool
    spec:
      containers:
      - name: openfda-tool
        image: openfda-tool:latest
        imagePullPolicy: Never  # For local Kind cluster
        ports:
        - containerPort: 8374
          name: http
        envFrom:
        - configMapRef:
            name: openfda-tool-env-config
            optional: true  # Created by setup.sh from .env values
        env:
        - name: PORT
          value: "8374"
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: REDIS_URL
          value: "redis://redis.truvag3-examples:6379"
        # Redis Registration Resilience (handles Redis startup race conditions)
        - name: TRUVAG3_DISCOVERY_RETRY
          value: "true"
        - name: TRUVAG3_DISCOVERY_RETRY_INTERVAL
          value: "30s"
        # Optional: OPENFDA_API_KEY increases daily limit from 1K to 120K
        - name: OPENFDA_API_KEY
          valueFrom:
            secretKeyRef:
              name: openfda-tool-secrets
              key: OPENFDA_API_KEY
              optional: true  # Tool works without key (lower rate limit)
        - name: DEV_MODE
          value: "false"
        - name: TRUVAG3_LOG_LEVEL
          value: "info"
        - name: TRUVAG3_LOG_FORMAT
          value: "json"
        - name: TRUVAG3_K8S_SERVICE_NAME
          value: "openfda-tool-service"
        - name: TRUVAG3_K8S_SERVICE_PORT
          value: "80"
        - name: TRUVAG3_K8S_POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        # Telemetry Configuration
        - name: APP_ENV
          value: "development"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://otel-collector.truvag3-examples:4318"
        resources:
          requests:
            cpu: "10m"
            memory: "16Mi"
          limits:
            cpu: "100m"
            memory: "64Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8374
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /api/capabilities
            port: 8374
          initialDelaySeconds: 5
          periodSeconds: 10
